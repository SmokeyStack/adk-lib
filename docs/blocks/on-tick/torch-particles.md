# adk-lib:on_tick_torch_particles

## What does it do?

This component will spawn flame particles for the `adk::BlockTorch` class. This component has no customization.

## How to use

Add `adk-lib:on_tick_torch_particles` to the `minecraft:custom_components` array in your item json file.
