# adk-lib:on_step_off_disappearing

## What does it do?

This component will make the block disappear then reappear. This component has no customization.

## How to use

Add `adk-lib:on_step_off_disappearing` to the `minecraft:custom_components` array in your item json file.
