# adk-lib:on_step_off_effect

## What does it do?

This component will give the entity that steps off the block the speed effect. This component has no customization.

## How to use

Add `adk-lib:on_step_off_effect` to the `minecraft:custom_components` array in your item json file.
